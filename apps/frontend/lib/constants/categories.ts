/**
 * Category Constants
 *
 * Defines category-related constants and mappings
 */

import { ResourceCategory } from '../types/resource.types';

export const categoryLabels: Record<ResourceCategory, string> = {
  'meeting-room': 'Meeting Room',
  workspace: 'Workspace',
  equipment: 'Equipment',
  venue: 'Venue',
  vehicle: 'Vehicle',
};
